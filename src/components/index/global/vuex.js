import Vue from 'vue'
import Vuex from 'vuex'

Vue.use(Vuex)

const state = {
}

const mutations = {
}

const actions = {

}

const store = new Vuex.Store({
  strict: true,
  state: state,
  mutations: mutations,
  actions: actions,
})

export default store
